/* ---------------------------------------------------------------
   Artwork slots.

   All three are designed placeholders — gradient panels in the brand
   palette, not photographs. Every one sits behind a dark veil, so a
   real photo dropped in at the same path appears with no code change
   and needs no particular composition.

   To swap in real photography: replace the file, keep the name, keep
   it landscape and around 1200x800. Nothing else to change.
   --------------------------------------------------------------- */
export const IMG = {
  /* sign-in screen, and the head of the Tools tab */
  hero:   require('../assets/img/hero.jpg'),
  /* behind the quote card at the foot of Tools */
  quote:  require('../assets/img/quote.jpg'),
};
